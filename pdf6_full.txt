=== page 1 ===
객체지향언어
제6장 함수중복과 static 멤버
2026. 3. 25
컴퓨터공학
허훈식
=== page 2 ===
Learning Objectives
1. 함수 중복의 개념을 이해하고, 중복 함수를 작성할 수 있다.
2. 디폴트 매개 변수를 이해하고 작성할 수 있다.
3. 함수 중복 시 발생하는 모호성의 경우를 판별할 수 있다.
4. static 속성으로 선언된 멤버의 특성을 이해하고, static 속성을 활용할 수 있
다.
2
=== page 3 ===
제6장. 함수중복과 static 멤버
6.1 함수 중복
➢ 함수 중복(function overloading)
- 동일한 이름의 함수가 공존
• 다형성의 한 사례
• C 언어에서는 불가능 – 매개 변수를 제외한 함수 이름만으로 컴파일(이름 규칙 때문)
- 함수 중복이 가능한 범위
• 보통 함수들 사이
• 클래스의 멤버 함수들 사이
• 상속 관계에 있는 기본 클래스와 파생 클래스의 멤버 함수들 사이
➢ 함수 중복 조건
- 중복된 함수들의 이름 동일
- 중복된 함수들의 매개 변수 타입이 다르거나 개수가 달라야 함
- 리턴 타입은 함수 중복과 무관
3
=== page 4 ===
제6장. 함수중복과 static 멤버
6.1 함수 중복
➢ 함수 중복 성공 사례
중복된 sum() 함수 호출.
중복된 sum() 함수들 컴파일러가 구분
int sum(int a, int b, int c) {
return a + b + c;
} int main() {
cout << sum(2, 5, 33);
double sum(double a, double b) {
return a + b; cout << sum(12.5, 33.6);
}
cout << sum(2, 6);
int sum(int a, int b) { }
return a + b;
}
4
=== page 5 ===
제6장. 함수중복과 static 멤버
6.1 함수 중복
➢ 함수 중복 실패 사례
잘못 사용된 경우
int sum(int a, int b) {
return a + b;
} int main() {
? cout << sum(2, 5);
double sum(int a, int b) { }
return (double)(a + b);
}
컴파일러는 어떤 sum() 함수
를 호출하는지 구분할 수 없음
5
=== page 6 ===
제6장. 함수중복과 static 멤버
6.1 함수 중복
➢ [예제 6–1] big() 함수 중복 연습
- 큰 수를 리턴하는 다음 두 개의 big 함수를 중복 구현하라.
#include <iostream>
using namespace std;
int big(int a, int b) { // a와 b 중 큰 수 리턴
if(a>b) return a;
else return b;
}
int big(int a[], int size) { // 배열 a[]에서 가장 큰 수 리턴
int res = a[0];
for(int i=1; i<size; i++)
if(res < a[i]) res = a[i];
return res;
}
3
int main() {
9
int array[5] = {1, 9, -2, 8, 6};
cout << big(2,3) << endl;
cout << big(array, 5) << endl;
}
6
=== page 7 ===
제6장. 함수중복과 static 멤버
6.1 함수 중복
➢ [예제 6–2] sum() 함수 중복 연습
- 함수 sum()을 호출하는 경우가 다음과 같을 때, 함수 sum()을 중복 구현하라. sum()의
첫 번째 매개 변수는 두 번째 매개 변수보다 작은 정수 값으로 호출된다고 가정.
#include <iostream>
using namespace std;
int sum(int a, int b) { // a에서 b까지 합하기
int s = 0;
for(int i=a; i<=b; i++)
s += i;
return s;
}
int sum(int a) { // 0에서 a까지 합하기
int s = 0;
for(int i=0; i<=a; i++)
s += i;
12
return s;
}
6
int main() {
cout << sum(3, 5) << endl; 5050
cout << sum(3) << endl;
cout << sum(100) << endl;
}
7
=== page 8 ===
제6장. 함수중복과 static 멤버
6.1 함수 중복
➢ 생성자 함수 중복
- 객체 생성시, 매개 변수를 통해 다양한 형태의 초깃값 전달
class string { string 클래스의 사례
.....
public:
string(); // 빈 문자열을 가진 스트링 객체 생성
string(string& str); // str을 복사한 새로운 스트링 객체 생성
string(char* s); // ‘\0’로 끝나는 C-스트링 s를 스트링 객체로 생성
.....
};
string str; // 빈 문자열을 가진 스트링 객체
string copyAddress(address); // address의 문자열을 복사한 copyAddress 생성
string address("서울시 성북구 삼선동 389");
➢ 소멸자 함수 중복 불가
8
=== page 9 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ 디폴트 매개 변수(default parameter)
- 매개 변수에 값이 넘어오지 않는 경우, 디폴트 값을 받도록 선언된 매개 변수
➢ 디폴트 매개 변수 선언 사례
void star(int a=5); // a의 디폴트 값은 5
➢ 디폴트 매개 변수를 가진 함수 호출
star(); // 매개 변수 a에 디폴트 값 5가 전달됨. star(5);와 동일
star(10); // 매개 변수 a에 10을 넘겨줌
9
=== page 10 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ 디폴트 매개 변수 사례 1
void msg(int id, string text="Hello"); // text의 디폴트 값은 "Hello"
msg(10); // id에 10, text에 “Hello” 전달
msg(20, "Good Morning"); // id에 20, text에 “Good Morning” 전달
msg(); // 컴파일 오류. 첫 번째 매개 변수 id에 반드시 값을 전달하여야 함
msg("Hello"); // 컴파일 오류. 첫 번째 매개 변수 id에 값이 전달되지 않았음
호출 오류
10
=== page 11 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ 디폴트 매개 변수에 관한 제약 조건
- 디폴트 매개 변수는 보통 매개 변수 앞에 선언될 수 없음
- 디폴트 매개 변수는 끝 쪽에 몰려 선언되어야 함
void calc(int a, int b=5, int c, int d=0); // 컴파일 오류
void sum(int a=0, int b, int c); // 컴파일 오류
void calc(int a, int b=5, int c=0, int d=0); // 컴파일 성공
11
=== page 12 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ 매개변수에 값을 정하는 규칙 사례 2
void square(int width=1, int height=1); 디폴트 매개 변수를
가진 square()
void square(int width= 1, int height= 1);
square( ); square( , ); square( 1, 1 );
square(5); square( 5, ); square( 5, 1 );
square(3, 8); square( 3, 8 ); square( 3, 8 );
컴파일러에 의해
변환되는 과정
12
=== page 13 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ 매개변수에 값을 정하는 규칙 사례 3
void g(int a, int b=0, int c=0, int d=0); 디폴트 매개 변수를
가진 square()
void g(int a, int b= 0, int c= 0, int d= 0);
g(10); g( 10, , , ); g( 10, 0, 0, 0 );
g(10, 5); g( 10, 5 , , ); g( 10, 5, 0, 0 );
g(10, 5, 20); g( 10, 5, 20, ); g( 10, 5, 20, 0 );
g(10, 5, 20, 30); g( 10, 5, 20, 30 ); g( 10, 5, 20, 30 );
컴파일러에 의해
변환되는 과정
13
=== page 14 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ [예제 6–3] 디폴트 매개 변수를 가진 함수 선언 및 호출
#include <iostream>
#include <string>
using namespace std; 디폴트
// 원형 선언 매개 변수 선언
void star(int a=5);
void star(int a=5) {
void msg(int id, string text="");
for(int i=0; i<a; i++)
// 함수 구현
void star(int a) { cout << '*’;
for(int i=0; i<a; i++)
cout << endl;
cout << '*';
}
cout << endl;
} 동일한
void msg(int id, string text) { 코드 void msg(int id, string text="") {
cout << id << ' ' << text << endl;
cout << id << ' ' << text << endl;
}
}
int main() {
// star() 호출
star(); star(5);
star(10); *****
// msg() 호출 **********
msg(10);
msg(10, “”);
msg(10, "Hello"); 10
}
10 Hello
14
=== page 15 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ [예제 6–4] 디폴트 매개 변수를 가진 함수 만들기 연습
- 함수 f()를 호출하는 경우, f()를 디폴트 매개 변수를 가진 함수로 작성하라.
#include <iostream>
using namespace std;
// 원형 선언
void f(char c=' ', int line=1);
// 함수 구현
void f(char c, int line) {
빈 칸이 10개
for(int i=0; i<line; i++) { 출력됨
for(int j=0; j<10; j++)
cout << c;
cout << endl; %%%%%%%%%%
} @@@@@@@@@@
} @@@@@@@@@@
int main() { @@@@@@@@@@
f(); // 한줄에 빈칸을 10개 출력한다. @@@@@@@@@@
f('%'); // 한 줄에 '%'를 10개 출력한다. @@@@@@@@@@
f('@', 5); // 5 줄에 '@' 문자를 10개 출력한다.
}
15
=== page 16 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ 디폴트 매개 변수의 장점 – 함수 중복 간소화
class Circle { 2 개의 생성자 함수를
디폴트 매개 변수를 가진 class Circle {
........................
하나의 함수로 간소화 ........................
public:
public:
Circle() { radius = 1; }
Circle(int r=1) { radius = r; }
Circle(int r) { radius = r; }
........................
........................
};
};
➢ 중복 함수들과 디폴트 매개 변수를 가진 함수를 함께 사용 불가
class Circle {
........................
public:
Circle() { radius = 1; }
Circle(int r) { radius = r; }
중복된 함수와
Circle(int r=1) { radius = r; }
동시 사용 불가
........................
};
16
=== page 17 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ [예제 6–5] 디폴트 매개 변수를 이용하여 중복 함수 간소화 연습
- 다음 두 개의 중복 함수를 디폴트 매개 변수를 가진 하나의 함수로 작성
void fillLine() { // 25 개의 '*' 문자를 한 라인에 출력
for(int i=0; i<25; i++) cout << '*';
cout << endl;
}
void fillLine(int n, char c) { // n개의 c 문자를 한 라인에 출력
for(int i=0; i<n; i++) cout << c;
cout << endl;
}
#include <iostream>
using namespace std;
void fillLine(int n=25, char c='*') { // n개의 c 문자를 한 라인에 출력
for(int i=0; i<n; i++) cout << c;
cout << endl; *************************
}
%%%%%%%%%%
int main() {
fillLine(); // 25개의 '*'를 한 라인에 출력
fillLine(10, '%'); // 10개의 '%'를 한 라인에 출력
}
17
=== page 18 ===
제6장. 함수중복과 static 멤버
6.2 디폴트 매개변수
➢ [예제 6–6] 디폴트 매개 변수를 이용하여 중복 함수 간소화 연습
- 다음 클래스에 중복된 생성자를 디폴트 매개 변수를 가진 하나의 생성자로 작성
#include <iostream>
class MyVector{
using namespace std;
int *p;
int size;
class MyVector{
public:
int *p;
MyVector() {
int size;
p = new int [100]; public:
size = 100; MyVector(int n=100) {
} p = new int [n];
MyVector(int n) { size = n;
}
p = new int [n];
~MyVector() { delete [] p; }
size = n;
};
}
int main() {
~MyVector() { delete [] p; }
MyVector *v1, *v2;
};
v1 = new MyVector(); // 디폴트로 정수 100개의 배열 동적 할당
v2 = new MyVector(1024); // 정수 1024개의 배열 동적 할당
delete v1;
delete v2;
}
18
=== page 19 ===
제6장. 함수중복과 static 멤버
6.3 함수 중복의 모호성
➢ 함수 중복이 모호하여 컴파일러가 어떤 함수를 호출하는지 판단하지 못하는
경우
- 형 변환으로 인한 모호성
- 참조 매개 변수로 인한 모호성
- 디폴트 매개 변수로 인한 모호성
19
=== page 20 ===
제6장. 함수중복과 static 멤버
6.3 함수 중복의 모호성
➢ 형 변환으로 인한 함수 중복의 모호성
- 매개 변수의 형 변환으로 인한 중복 함수 호출의 모호성
(a) 정상 컴파일 (b) 모호한 호출, 컴파일 오류
float square(float a) {
return a*a;
}
double square(double a) {
return a*a;
double square(double a) {
}
return a*a;
int main() {
int타입3이 }
cout << square(3); double 로 자
int main() {
} 동 형 변환
cout << square(3.0);
int타입3을
cout << square(3); double로 변환
} 할지 float로 변
환할 지 모호함
3.0은 double
타입이므로 모
호하지 않음
20
=== page 21 ===
제6장. 함수중복과 static 멤버
6.3 함수 중복의 모호성
➢ [예제 6–7] 형 변환으로 인해 함수 중복이 모호한 경우
#include <iostream>
using namespace std;
float square(float a) {
return a*a;
}
double square(double a) {
return a*a;
}
int main() {
cout << square(3.0); // square(double a); 호출
cout << square(3); // 컴파일 오류
}
21
=== page 22 ===
제6장. 함수중복과 static 멤버
6.3 함수 중복의 모호성
➢ [예제 6–8] 참조 매개 변수로 인한 함수 중복의 모호성
#include <iostream>
using namespace std;
두 함수는
int add(int a, int b) {
근본적으로
return a + b;
중복 시킬 수
없다. }
int add(int a, int &b) {
b = b + a;
return b;
}
call by value인지
int main(){
call by reference인지 모호
int s=10, t=20;
cout << add(s, t); // 컴파일 오류
}
22
=== page 23 ===
제6장. 함수중복과 static 멤버
6.3 함수 중복의 모호성
➢ [예제 6–9] 디폴트 매개 변수로 인한 함수 중복의 모호성
#include <iostream>
#include <string>
using namespace std;
void msg(int id) {
cout << id << endl;
}
void msg(int id, string s="") {
cout << id << ":" << s << endl;
? ? }
int main(){
디폴트 매개 변 msg(5, "Good Morning"); // 정상 컴파일. 두 번째 msg() 호출
수를 이용하고
msg(6); // 함수 호출 모호. 컴파일 오류
있는지 모호함
}
23
=== page 24 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static
- 변수와 함수에 대한 기억 부류의 한 종류
• 생명 주기 – 프로그램이 시작될 때 생성, 프로그램 종료 시 소멸
• 사용 범위 – 선언된 범위, 접근 지정에 따름
➢ 클래스의 멤버
- static 멤버
• 프로그램이 시작할 때 생성
• 클래스 당 하나만 생성, 클래스 멤버라고 불림
• 클래스의 모든 인스턴스(객체)들이 공유하는 멤버
- non-static 멤버
• 객체가 생성될 때 함께 생성
• 객체마다 객체 내에 생성
• 인스턴스 멤버라고 불림
24
=== page 25 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ 멤버의 static 선언
- 멤버 변수나 멤버 함수의 선언문에 static 지정자를 붙인다.
➢ static 멤버 변수 생성
- 전역 변수로 생성, 전체 프로그램 내에 한 번만 생성
class Person {
public:
double money; // 개인 소유의 돈
void addMoney(int money) { non-static 멤버 선언
this->money += money;
static 멤버 변수 선언 }
static int sharedMoney; // 공금
static 멤버 함수 선언 static void addShared(int n) {
sharedMoney += n;
} static 변수 공간 할당.
프로그램의 전역 공간에 선언
};
int Person::sharedMoney = 10; // sharedMoney를 10으로 초기화
➢ static 멤버 변수는 외부에 전역변수로 선언되어야 한다. 없으면 링크 오류
25
=== page 26 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버와 non-static 멤버의 관계
static 멤버는 하나만
생성되고 모든
Person han, lee, park, choi; 객체들에 의해 공유됨
static 멤버 공유
money
money
sharedMoney 10
addMoney() { ... }
addMoney() { ... }
addShared() { ... }
han
choi
money money
non-static
멤버는 객체마다
addMoney() { ... } addMoney() { ... }
생성됨
lee park
• han, lee, park, choi 등 4 개의 Person 객체 생성
• sharedMoney와 addShared() 함수는 하나만 생성되고 4 개의 객체들의 의해 공유됨
• sharedMoney와 addShared() 함수는 han, lee, park. choi 객체들의 멤버임
26
=== page 27 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버와 non-static 멤버 비교
27
=== page 28 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버 사용 : 객체의 멤버로 접근
- static 멤버는 객체 이름이나 객체 포인터로 접근
• 보통 멤버처럼 접근할 수 있음
객체.static멤버
객체포인터->static멤버
• Person 타입의 객체 lee와 포인터 p를 이용하여 static 멤버를 접근하는 예
Person lee;
lee.sharedMoney = 500; // 객체.static멤버 방식
Person *p;
p = &lee;
p->addShared(200); // 객체포인터->static멤버 방식
28
=== page 29 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버와 non-static 멤버 비교
29
=== page 30 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버 사용
- 클래스 이름과 범위 지정 연산자(::)로 접근 가능
• 클래스명::static멤버
• static 멤버는 클래스마다 오직 한 개만 생성되기 때문
han.sharedMoney = 200; <-> Person::sharedMoney = 200;
lee.addShared(200); <-> Person::addShared(200);
• non-static 멤버는 클래스 이름으로 접근 불가
// 컴파일 오류. non-static 멤버는 클래스 명으로 접근 불가
Person::money = 100;
// 컴파일 오류. non-static 멤버는 클래스 명으로 접근 불가
Person::addMoney(200);
30
=== page 31 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버와 non-static 멤버 비교
31
=== page 32 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static의 주요 활용
- 전역 변수나 전역 함수를 클래스에 캡슐화
• 전역 변수나 전역 함수를 가능한 사용하지 않도록
• 전역 변수나 전역 함수를 static으로 선언하여 클래스 멤버로 선언
- 객체 사이에 공유 변수를 만들고자 할 때
• static 멤버를 선언하여 모든 객체들이 공유
32
=== page 33 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ [예제 6–10] static 멤버를 가진 Math 클래스 작성
- 왼쪽 코드를 static 멤버를 가진 Math 클래스로 작성하고 멤버 함수를 호출하라
(b) Math 클래스를 만들고 전역 함수들을
(a) 전역 함수들을 가진 좋지 않은 코딩 사례
static 멤버로 캡슐화한 프로그램
#include <iostream>
using namespace std;
#include <iostream>
using namespace std;
class Math {
public:
int abs(int a) { return a>0?a:-a; }
static int abs(int a) { return a>0?a:-a; }
int max(int a, int b) { return a>b)?a:b; }
static int max(int a, int b) { return (a>b)?a:b; }
int min(int a, int b) { return (a>b)?b:a; }
static int min(int a, int b) { return (a>b)?b:a; }
};
int main() {
cout << abs(-5) << endl;
int main() {
cout << max(10, 8) << endl; 5
cout << Math::abs(-5) << endl;
cout << min(-3, -8) << endl;
cout << Math::max(10, 8) << endl; 10
}
cout << Math::min(-3, -8) << endl;
-8
}
33
=== page 34 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ [예제 6–11] static 멤버를 공유의 목적으로 사용하는 예
class Circle {
private: 생존하고 있는 원의 개수 = 10
static int numOfCircles; 생존하고 있는 원의 개수 = 0
int radius; 생존하고 있는 원의 개수 = 1
public: 생존하고 있는 원의 개수 = 2
Circle(int r=1);
~Circle() { numOfCircles--; } // 생성된 원의 개수 감소
double getArea() { return 3.14*radius*radius;}
static int getNumOfCircles() { return numOfCircles; }
};
Circle::Circle(int r) {
radius = r;
numOfCircles++; // 생성된 원의 개수 증가
}
int Circle::numOfCircles = 0; // 0으로 초기화
int main() {
생성자가 10번 실행되어
numOfCircles = 10 이 됨 Circle *p = new Circle[10]; // 10개의 생성자 실행
cout << "생존하고 있는 원의 개수 = " << Circle::getNumOfCircles() << endl;
numOfCircles = 0 이 됨 delete [] p; // 10개의 소멸자 실행
cout << "생존하고 있는 원의 개수 = " << Circle::getNumOfCircles() << endl;
numOfCircles = 1 이 됨 Circle a; // 생성자 실행
cout << "생존하고 있는 원의 개수 = " << Circle::getNumOfCircles() << endl;
numOfCircles = 2 가 됨 Circle b; // 생성자 실행
cout << "생존하고 있는 원의 개수 = " << Circle::getNumOfCircles() << endl;
}
34
=== page 35 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버 함수는 static 멤버만 접근 가능
- static 멤버 함수가 접근할 수 있는 것
• static 멤버 함수
• static 멤버 변수
• 함수 내의 지역 변수
➢ static 멤버 함수는 non-static 멤버에 접근 불가
- 객체가 생성되지 않은 시점에서 static 멤버 함수가 호출될 수 있기 때문
35
=== page 36 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버 함수 getMoney()가 non-static 멤버 변수 money 접근 오류
static intgetMoney() {
컴파일 오류.
main()이 시작하기 전 return money;
class PersonError {
static 멤버 함수는 non-static
}
int money; 멤버에 접근할 수 없음.
money는 아직
public: 생성되지 않았음.
static int getMoney() { return money; }
static intgetMoney() {
void setMoney(int money) { // 정상 코드 n = PersonError::getMoney(); return money;
}
this->money = money;
생성되지 않는 변수를
}
접근하게 되는 오류를
}; 범함
int main(){
static intgetMoney() {
int n = PersonError::getMoney(); return money;
PersonError errorKim;
}
PersonError errorKim;
errorKim
money
errorKim.setMoney(100);
} errorKim객체가 생길 때 setMoney() { ... }
money가 비로소 생성됨
36
=== page 37 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ non-static 멤버 함수는 static에 접근 가능
class Person {
public:
double money; // 개인 소유의 돈
static int sharedMoney; // 공금
....
int total() { // non-static 함수는 non-static이나 static 멤버에 모두 접근 가능
return money + sharedMoney;
}
non-static static
};
37
=== page 38 ===
제6장. 함수중복과 static 멤버
6.4 static 멤버
➢ static 멤버 함수는 this 사용 불가
- static 멤버 함수는 객체가 생기기 전부터 호출 가능
class Person {
public:
double money; // 개인 소유의 돈
static int sharedMoney; // 공금
....
static void addShared(int n) { // static 함수에서 this 사용 불가
this->sharedMoney + = n; // this를 사용하므로 컴파일 오류
}
}; sharedMoney+= n;으로 하면 정상 컴파일
38
